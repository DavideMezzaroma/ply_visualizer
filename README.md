# PLY file parser and visualizer
A small .ply file visualizer written entirely in C using raylib.
The input files **must** be exported enabling ply ASCII format.

example usage:
`./ply_parser <file.ply>`
